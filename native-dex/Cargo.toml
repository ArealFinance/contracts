[package]
name = "native-dex"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
arlex-lang = { path = "../../../Solana/arlex/framework/arlex-lang" }

[lib]
crate-type = ["cdylib", "lib"]
