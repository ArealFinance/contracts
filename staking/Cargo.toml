[package]
name = "staking"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
arlex-lang = { workspace = true }

[dev-dependencies]
sha2 = { workspace = true }
bs58 = { workspace = true }

[features]
# Selects the devnet program ID in declare_id! (keys/devnet/staking.json).
devnet = []

[lib]
crate-type = ["cdylib", "lib"]
