[package]
name = "yield-distribution"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
arlex-lang = { path = "../../../Solana/arlex/framework/arlex-lang" }
sha2 = { version = "0.10", default-features = false }

[lib]
crate-type = ["cdylib", "lib"]

[profile.release]
opt-level = "z"
lto = true
